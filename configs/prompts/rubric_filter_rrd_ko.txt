다음 기준이 sampled responses를 구분하는 데 적절한지 판단하세요.

제거해야 하는 기준:
- 사용자 요청과 무관함
- response만 보고 판정 불가
- 거의 모든 sampled responses에 동시에 적용되어 구별력이 낮음
- 다른 기준과 거의 중복됨
- 한국어 평가 기준으로 너무 모호하거나 부자연스러움

반드시 JSON만 출력하세요.

{
  "keep": true,
  "reason": "짧은 이유"
}

<user_prompt>
{prompt}
</user_prompt>

<criterion>
{criterion_json}
</criterion>

<auxiliary_sample_responses>
{sample_responses_json}
</auxiliary_sample_responses>

